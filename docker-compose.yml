services:
  app:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    environment:
      - PORT=8000
    env_file:
      - .env
    volumes:
      - ./data:/app/data:ro
      - ./files:/app/files
    restart: unless-stopped
