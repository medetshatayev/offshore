services:
  app:
    build: .
    ports:
      - "${PORT:-8008}:${PORT:-8008}"
    env_file:
      - .env
    volumes:
      - ./data:/app/data:ro
      - /home/dladmin/offshore/files:/app/files
    restart: unless-stopped
